# Pokegents Requirements

Pokegents is a local-first dashboard and CLI for running multiple coding agents with project + role configuration, live status, session search, and inter-agent messaging.

Current public requirements are tracked in `README.md` and implementation-specific architecture notes in `ARCHITECTURE.md`. Older internal profile-era plans were removed before publication to avoid stale guidance.
