# Backend Decoupling Notes

Pokegents is designed to keep project/session identity separate from the underlying agent backend. Claude and Codex can be configured as backends; future backends should map native lifecycle events into the same local status model.

This document intentionally excludes private project examples and historical internal migration notes.
